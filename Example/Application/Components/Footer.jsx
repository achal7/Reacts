import React from 'react';
import Reacts from '../../../Reacts.js';

export default class Footer extends Reacts.Component(){
    render(){
        return (
            <div>Footer</div>
        );
    }
}